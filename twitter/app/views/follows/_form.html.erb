
<form action="<%= user_follow_url(user) %>" method="post">
  <button class="follow-toggle" type="button" name="button" data-user-id="<%= user.id %>" data-initial-follow-state="<%= current_user.follows?(user) ? "followed" : "unfollowed"%>"></button>
</form>
